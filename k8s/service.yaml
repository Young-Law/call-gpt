apiVersion: v1
kind: Service
metadata:
  name: call-gpt
  labels:
    app: call-gpt
spec:
  type: ClusterIP
  selector:
    app: call-gpt
  ports:
    - name: http
      port: 80
      targetPort: http
